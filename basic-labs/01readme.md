creating a basic network using:
- 1 Router
- 2 Switches
- 4 PCs(2 PCs on each switch)

assigning an ip addresses to all the pcs manually and then setting up IP for two different interfaces of the router and setting up default gateways in all the PCs